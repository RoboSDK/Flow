#include "flow/registry.hpp"
#include <catch2/catch.hpp>

TEST_CASE("Test subscription and publishing", "[make_array]")
{
//  auto channels = flow::registry{};
//
//  struct Point {};
//  constexpr auto channel_name = "point";
//  const auto on_point_message = []([[maybe_unused]] Point const& p) {};
//
//  flow::subscribe<Point>(channel_name, channels, on_point_message);
//
//  REQUIRE(channels.has_channel<Point>());
}
